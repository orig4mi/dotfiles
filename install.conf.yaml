- defaults:
    link:
        relink: true
        create: true

- clean: ['~']

- link:
    ~/.aliases:
    ~/.bashrc:
    ~/.dotfiles: ''
    ~/.gitconfig: 
    ~/.oh-my-zsh:
    ~/.profile:
    ~/.shell.pre-oh-my-zsh:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:

- create:
    - ~/.vim/undo-history

- shell:
    - git submodule sync --recursive
    - git submodule update --init --recursive
